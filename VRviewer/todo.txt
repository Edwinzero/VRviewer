1 GetTrackedDeviceString																														done
2 BInit																																	  seems done

void APIENTRY DebugCallback(GLenum source, GLenum type, GLuint id, GLenum severity, GLsizei length, const char* message, const void* userParam)
{
	dprintf( "GL Error: %s\n", message );
}

3 BInitGL																																  seems done 

// Purpose: Initialize Compositor. Returns true if the compositor was
//          successfully initialized, false otherwise.
4 BInitCompositor																																done

5 Shutdown																																		done

6 HandleInput																																	done
7 RunMainLoop																																	done
8 ProcessVREvent																																done

9 RenderFrame  == Render() in the project																										done
10 CreateAllShaders																																done
11 SetupTexturemaps																																done
12 SetupScene  (set cubes)																														done

13 RenderControllerAxes																														    done
14 SetupCameras																																	done
15 CreateFrameBuffer																															done

16 SetupStereoRenderTargets == (create 2 fbo for left and right eye)																			done
17 SetupCompanionWindow == (create window to contain HMD content)																				done
18 RenderStereoTargets == (render left and right eye using fbo)																					done
19 RenderScene == (render the scene from a single eye)																							done
20 RenderCompanionWindow == (render the companion window which is render 2 fbo to companion window)												done

21 GetHMDMatrixProjectionEye == utils																											done
22 GetHMDMatrixPoseEye == utils																													done
23 GetCurrentViewProjectionMatrix																												done

24 UpdateHMDMatrixPose == (update the transformation in HMD)																					done

25 FindOrLoadRenderModel ==   (Finds a render model we've already loaded or loads a new one) rendermodel here indicates VIVE devices			done
26 SetupRenderModelForTrackedDevice == (Create/destroy GL a Render Model for a single tracked device)											done
27 SetupRenderModels == (create / destroy GL render models)																						done
28 ConvertSteamVRMatrixToMatrix4 == (Converts a SteamVR matrix to our local matrix class) (TODO : Eigen + glm)									done

